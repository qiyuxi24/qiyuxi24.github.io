# RSS 订阅：原理 + 你的站点怎么用

> 一句话版本：**RSS 就是你的博客公开的一份"更新清单"文件（`feed.xml`），读者用阅读器定期来抓这份清单，你发新文章他们就能自动收到通知。** 你的站点是纯静态页，但 RSS 完全不需要服务器，靠"发布时生成静态文件"就够了——你的站点**已经实现了**，这篇文档解释它是什么、怎么工作的、以及你以后该怎么做。

---

## 一、RSS 是什么？（打三个比方）

**比方 1：订阅杂志**
你订阅一本杂志，杂志社每期出版后主动寄给你。RSS 类似：你"订阅"某个博客，博主每次发新文章，你的阅读器自动收到。

**比方 2：和"关注公众号"的区别**
公众号是"中心化"的：平台（微信）决定给你推什么。RSS 是"去中心化"的：你直接订阅博主自己的网站，没有中间平台，不受算法控制，也不会被限流。

**比方 3：菜市场公告栏**
你的网站门口挂一块公告栏（`feed.xml`），写了"最近新上了哪些菜（文章）"。读者（订阅者）隔一会儿就来看一眼公告栏，发现有新菜就来买。你不用挨个通知，公告栏挂在那里就行。

**技术上的本质**：RSS 是一种公开的**格式约定**（XML 文件，叫 `feed`）。全世界阅读器都认识这个格式，所以任何人的网站只要提供了 `feed.xml`，任何阅读器都能订阅。

---

## 二、整套流程长什么样（读者视角）

```
读者在阅读器里添加订阅地址
        │
        ▼
https://qiyuxi24.github.io/feed.xml   ← 你网站的"公告栏"
        │
        ▼
阅读器每隔 30 分钟~几小时自动来抓一次这个文件
        │
        ▼
发现有新文章 → 推送给读者 → 读者点链接跳回你的网站阅读
```

你唯一要提供的，就是那个 `feed.xml`。剩下全是阅读器的事。

**读者用哪些阅读器？** 比较常见的：Feedly（网页/App）、Inoreader、Reeder（苹果）、以及各类手机 App（如"小宇宙"不支持，但很多通用阅读器支持）。读者把 `https://qiyuxi24.github.io/feed.xml` 这个地址粘进去即可。

---

## 三、你的站点是怎么实现的？（作者视角）

### 关键认知：RSS 不需要服务器

很多人以为 RSS 需要后端实时推送，其实不是。RSS 只需要一个**内容会跟着新文章变化的静态文件**。你的 GitHub Pages 纯静态托管完全够用，流程是：

```
你写文章
   │  （保存时）
   ▼
content/posts/你的文章.md        ← 原始 Markdown
   │  build-index.mjs（生成清单）
   ▼
content/posts/index.json         ← 所有文章的"目录"
   │  build-feed.mjs（生成订阅源）
   ▼
feed.xml                        ← 就是那个"公告栏"，纯静态文件
   │  git push 到 GitHub
   ▼
GitHub Pages 托管（https://qiyuxi24.github.io/feed.xml）
   │  读者阅读器定期抓取
   ▼
读者收到更新通知
```

**核心思想**：`feed.xml` 不是"实时生成"的，而是在你**发布文章那一刻**由脚本生成好的。生成完之后它就是个普普通通的静态文件，和你的 `index.html`、图片一样被 GitHub Pages 托管。这就是"静态站也能有 RSS"的原因。

### 你的项目里有哪些东西参与？

| 文件 | 作用 |
|------|------|
| `feed.xml`（根目录） | 最终的 RSS 订阅源，就是那个"公告栏" |
| `scripts/build-feed.mjs` | 生成脚本：读 `content/posts/index.json` → 产出 `feed.xml` |
| `scripts/build-index.mjs` | 生成清单：扫 `content/posts/` 的 md → 产出 `index.json` |
| `scripts/publish-server.mjs` | 发布工作台后端，**保存/删除文章时自动执行上面两个脚本** |
| `index.html` 博客页订阅条 | 页面上那个"订阅 RSS"按钮，链接到 `./feed.xml` |
| `index.html` 的 `<link rel="alternate" ...>` | 告诉浏览器/阅读器"本站有 RSS 源"（自动发现） |

### feed.xml 里面到底有什么？

就是一堆标准格式的"文章卡片"（标题、链接、日期、摘要）。你现在这个文件里就有 3 篇文章，结构大致是：

```xml
<item>
  <title>不一样的普通人：从标签到锚点</title>
  <link>https://qiyuxi24.github.io/reader.html?slug=ordinary-people</link>
  <pubDate>Thu, 25 Jun 2026 00:00:00 GMT</pubDate>
  <description>我们既渴望与众不同，又不想承认自己是七十亿分之一。</description>
  <category>随笔</category>
</item>
```

阅读器认的就是这套固定格式，全世界通用。

> 小知识：为什么链接是 `reader.html?slug=xxx` 而不是 `xxx.html`？因为你的站是纯静态页，每篇文章没有独立文件，而是共用 `reader.html` 这一个页面，用 URL 参数 `?slug=` 告诉它"显示哪篇"。RSS 里记录的链接只要能打开文章就行，形式不限。

---

## 四、具体怎么做（操作手册）

### 4.1 读者怎么订阅你的站（把这个告诉读者即可）

订阅地址（唯一需要的东西）：

```
https://qiyuxi24.github.io/feed.xml
```

以 Feedly 为例：打开 feedly.com → 登录 → 搜索框粘贴上面地址 → 添加 → 完成。手机 App 同理，一般叫"添加订阅 / Add feed"。

> 如果你的站以后换了域名，订阅地址会变，老读者需要重新订阅。这也是 RSS 唯一的小缺点。

### 4.2 你发新文章后，RSS 会自动更新吗？

**用发布工作台（推荐方式）→ 自动更新，你什么都不用做。**

发布工作台（`start-publish.bat` → `http://localhost:3456/`）点「保存并发布」时，后端会自动帮你执行：

1. 写 `content/posts/你的文章.md`
2. 重建 `content/posts/index.json`
3. 重建 `feed.xml`（RSS 自动包含新文章）

然后把改动 `git add . && git commit && git push` 推上去，读者就能订阅到新文章了。

**用命令行（手动方式）→ 改完文章后跑两条命令：**

```bash
node scripts/build-index.mjs   # 重建文章清单
node scripts/build-feed.mjs    # 重建 RSS（含新文章）
```

### 4.3 怎么验证 RSS 是正常的？

三种办法，从简单到严格：

1. **浏览器直接打开** `https://qiyuxi24.github.io/feed.xml`——能看到新文章的条目就正常（浏览器可能显示成 XML 树，正常的）。
2. **自己订阅一次**：拿手机装个 RSS 阅读器，添加你的地址，发一篇文章推上去，看能否收到。
3. **用官方校验工具**：打开 https://validator.w3.org/feed/ ，粘贴 feed 地址校验格式是否标准。

### 4.4 发文章但暂时不想让订阅者看到？

在文章的 front-matter 里写 `published: false`，它就不会进 `index.json` 和 `feed.xml`（草稿功能）。

---

## 五、常见问题（FAQ）

**Q1：读者说订阅了但看不到新文章？**
- 阅读器不是实时的，通常 30 分钟~几小时才抓一次，让它等一下或手动刷新。
- 确认你已经把新文章的改动 **git push** 上去了（本地生成 ≠ 线上更新）。
- GitHub Pages 可能有 CDN 缓存，一般几分钟内更新，最迟几小时。

**Q2：GitHub Pages 会不会因为纯静态而做不了 RSS？**
不会。`feed.xml` 本身就是静态文件，和图片、CSS 没有区别，托管毫无障碍。你担心的"实时推送"从来不是 RSS 的要求。

**Q3：RSS 和"邮件订阅"哪个好？**
RSS 不需要读者留下邮箱（更轻、更隐私），但需要读者会用阅读器。对技术型读者（你的目标读者）RSS 是标配。将来想要更大众的方式，可以再加邮件订阅（但那是另一个话题，需要第三方服务如 Buttondown/竹白）。

**Q4：我文章更新不频繁，还有必要保留吗？**
留着零成本（发布时自动更新，不需要维护），读者不活跃也不影响你。它是"认真做内容"的信号。唯一硬性要求是**别断更太久**——超过几个月不更新，订阅者会流失。这是内容问题，不是技术问题。

**Q5：会不会泄露没发布的草稿？**
不会。`published: false` 的文章不会进 feed。真正没想好的内容不放 `content/posts/` 就行。

---

## 六、结论：删还是不删？

**建议保留。** 理由：

1. **已经实现了**——`feed.xml` 已在仓库里正常工作，包含全部已发布文章。
2. **零维护成本**——发布工作台保存文章时自动重建，你不需要做任何额外操作。
3. **纯静态站完全支持**——不需要服务器，GitHub Pages 托管一个静态 XML 即可。
4. **是加分项**——博客带 RSS 订阅入口是"内容创作者专业感"的标志。

**唯一前提**：保持更新频率（至少别几个月不写）。这取决于你，不取决于技术。

> 如果你最终仍想删：删掉博客页的订阅条（`index.html` 约 791-797 行）、`feed.xml`、`scripts/build-feed.mjs`，以及 `publish-server.mjs` 里自动重建 feed 的两行即可。但既然它能用，删了挺可惜的。
